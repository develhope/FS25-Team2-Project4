import classes from "./Settings.module.scss"

function Settings() {
    return (
        <div className={classes.settingsPage}>
            <h1>Settings</h1>
        </div>
    )
}

export default Settings
